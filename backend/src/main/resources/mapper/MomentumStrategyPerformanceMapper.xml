<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fund.analysis.mapper.MomentumStrategyPerformanceMapper">

    <!-- 查询所有每日绩效（按日期正序） -->
    <select id="selectAllOrderByDateAsc" resultType="com.fund.analysis.entity.MomentumStrategyPerformance">
        SELECT * FROM momentum_strategy_performance
        ORDER BY performance_date ASC, create_time ASC
    </select>

    <!-- 查询指定日期范围内的每日绩效 -->
    <select id="selectByDateRange" resultType="com.fund.analysis.entity.MomentumStrategyPerformance">
        SELECT * FROM momentum_strategy_performance
        WHERE performance_date &gt;= #{startDate}
        AND performance_date &lt;= #{endDate}
        ORDER BY performance_date ASC, create_time ASC
    </select>

    <!-- 删除指定日期范围内的每日绩效 -->
    <delete id="deleteByDateRange">
        DELETE FROM momentum_strategy_performance
        WHERE performance_date &gt;= #{startDate}
        AND performance_date &lt;= #{endDate}
    </delete>

</mapper>
