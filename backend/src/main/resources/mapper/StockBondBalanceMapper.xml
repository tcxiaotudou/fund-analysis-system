<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fund.analysis.mapper.StockBondBalanceMapper">
    
    <!-- 查询最新的股债平衡建议 -->
    <select id="selectLatest" resultType="com.fund.analysis.entity.StockBondBalance">
        SELECT * FROM stock_bond_balance 
        ORDER BY data_time DESC
        LIMIT 1
    </select>
    
    <!-- 删除旧数据 -->
    <delete id="deleteOldData">
        DELETE FROM stock_bond_balance 
        WHERE id NOT IN (
            SELECT id FROM (
                SELECT id FROM stock_bond_balance 
                ORDER BY data_time DESC 
                LIMIT #{keepCount}
            ) tmp
        )
    </delete>
    
</mapper>
